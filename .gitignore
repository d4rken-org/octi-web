node_modules
dist
.vite
.svelte-kit
*.log
.DS_Store
.idea/
.vscode/

# Playwright + bootstrap-peer artifacts. The committed screenshots/ tree is
# populated by the release-tag.yml workflow via the screenshots/refresh PR;
# local Playwright runs land in the same path and shouldn't be staged.
bootstrap-peer.json
test-results/
playwright-report/

# Cross-repo wire-format fixtures fetched from d4rken-org/octi at the SHA
# pinned in fixture-lock.json. Populated by `pnpm fixtures:sync` (and
# vitest's globalSetup before tests). Never edit by hand.
.cache/interop-fixtures/

